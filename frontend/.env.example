# Vite 開發/預覽時 /api 的 proxy 目標（backend 實際監聽位址）
# 未設定時預設 http://127.0.0.1:8082
VITE_API_PROXY_TARGET=http://127.0.0.1:8082

# 部署情境：若 frontend 被靜態託管、後端在別的 origin，可用這個覆寫 fetch 的 base URL
# 未設定時 frontend 走相對路徑 /api/*（同 origin + proxy / 反向代理）
# VITE_API_BASE_URL=https://api.example.com
